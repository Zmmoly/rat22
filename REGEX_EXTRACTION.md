# 🎯 استخراج الأوامر الذكي - Regex Based

## ✨ النهج الجديد:

**بدلاً من:** حذف كلمات معينة
**الآن:** استخراج الأوامر مباشرة باستخدام Regex - يتجاهل كل شيء آخر تلقائياً!

---

## 🧠 كيف يعمل؟

### النهج القديم (حذف كلمات):
```
النص: "لو سمحت افتح واتساب"
1. يحذف "لو سمحت"
2. النتيجة: "افتح واتساب"
3. يختبر الأمر

المشكلة: لا يمكن حذف كل الكلمات الممكنة!
```

### النهج الجديد (Regex):
```
النص: "لو سمحت افتح واتساب"
1. يبحث عن نمط: "افتح [اسم]"
2. يستخرج: "افتح واتساب"
3. يختبر الأمر

المميزة: يتجاهل كل شيء آخر تلقائياً!
```

---

## 📋 أمثلة:

### مثال 1: كلام كثير قبل الأمر
```
المدخل: أنا الآن أريد أن أفتح واتساب من فضلك يا رب

Regex يبحث عن: (?:افتح|شغل|فتح)\s+([^،,\n]+?)
يجد: "افتح واتساب"
يتجاهل: "أنا الآن أريد أن" و "من فضلك يا رب"

النتيجة: ✅ تم فتح واتساب
```

---

### مثال 2: أوامر متعددة مع كلام كثير
```
المدخل: لو سمحت، محتاج افتح واتساب، وبعدين على الصوت، وأخيراً شغل الواي فاي

Regex للأمر 1: يجد "افتح واتساب"
Regex للأمر 2: يجد "على الصوت"  
Regex للأمر 3: يجد "شغل الواي فاي"

يتجاهل: "لو سمحت"، "محتاج"، "وبعدين"، "وأخيراً"

النتيجة:
🔄 وجدت 3 أوامر
✅ تم فتح واتساب
✅ تم رفع الصوت
✅ فتح إعدادات الواي فاي
```

---

### مثال 3: الاتصال
```
المدخل: أنا محتاج ضروري أتصل بأحمد الآن

Regex: (?:اتصل\s+ب|اضرب\s+ل|اتصل|كلم)\s+([^،,\n]+?)
يجد: "اتصل بأحمد"
يتجاهل: "أنا محتاج ضروري" و "الآن"

النتيجة: 📞 اتصال مباشر بـ أحمد
```

---

### مثال 4: إغلاق تطبيق
```
المدخل: هل يمكنك أن تقفل لي واتساب لو سمحت؟

Regex: (?:أقفل|اقفل|سكر)\s+([^،,\n]+?)
يجد: "أقفل واتساب"
يتجاهل: "هل يمكنك أن" و "لي" و "لو سمحت؟"

النتيجة: ✅ جاري إغلاق واتساب...
```

---

## 🔧 الأنماط المستخدمة:

### 1. فتح التطبيقات
```kotlin
Regex("(?:افتح|شغل|فتح)\\s+([^،,\\n]+?)(?=\\s*(?:[،,\\n]|ثم|و(?=\\s)|$))")
```
**يستخرج:**
- "افتح واتساب" من "لو سمحت افتح واتساب"
- "شغل يوتيوب" من "ممكن تشغل يوتيوب؟"

---

### 2. إغلاق التطبيقات
```kotlin
Regex("(?:أقفل|اقفل|سكر)\\s+([^،,\\n]+?)(?=\\s*(?:[،,\\n]|ثم|و(?=\\s)|$))")
```
**يستخرج:**
- "أقفل واتساب" من "أريد أقفل واتساب"

---

### 3. الاتصال
```kotlin
Regex("(?:اتصل\\s+ب|اضرب\\s+ل|اتصل|كلم)\\s+([^،,\\n]+?)(?=\\s*(?:[،,\\n]|ثم|و(?=\\s)|$))")
```
**يستخرج:**
- "اتصل بأحمد" من "محتاج اتصل بأحمد الآن"
- "اضرب لأمي" من "لو سمحت اضرب لأمي"

---

### 4. التحكم في الصوت
```kotlin
Regex("(على\\s+الصوت|خفض\\s+الصوت|كتم\\s+الصوت)")
```
**يستخرج:**
- "على الصوت" من "أريد على الصوت من فضلك"

---

### 5. الإعدادات
```kotlin
Regex("(شغل\\s+الواي\\s+فاي|اطفي\\s+الواي\\s+فاي|شغل\\s+البلوتوث|اطفي\\s+البلوتوث)")
```
**يستخرج:**
- "شغل الواي فاي" من "ممكن تشغل الواي فاي؟"

---

### 6. أوامر النظام
```kotlin
Regex("(رجوع|ارجع|back|هوم|home|الشاشة\\s+الرئيسية|recent|التطبيقات\\s+الأخيرة)")
```
**يستخرج:**
- "رجوع" من "أريد رجوع"
- "هوم" من "اضغط هوم"

---

### 7. سكرين شوت
```kotlin
Regex("(سكرين\\s+شوت|لقطة\\s+شاشة|screenshot)")
```
**يستخرج:**
- "سكرين شوت" من "خذ لي سكرين شوت"

---

### 8. قراءة الشاشة
```kotlin
Regex("(اقرا\\s+الشاشة|ماذا\\s+في\\s+الشاشة)")
```
**يستخرج:**
- "اقرا الشاشة" من "هل يمكنك اقرا الشاشة؟"

---

### 9. الضغط على عناصر
```kotlin
Regex("(?:اضغط\\s+على|انقر\\s+على)\\s+(.+?)(?=\\s*(?:[،,\\n]|ثم|و(?=\\s)|$))")
```
**يستخرج:**
- "اضغط على موافق" من "لو سمحت اضغط على موافق"

---

### 10. الإشعارات
```kotlin
Regex("(?:افتح\\s+)?(?:الإشعارات|الاشعارات)")
```
**يستخرج:**
- "افتح الإشعارات" من "ممكن افتح الإشعارات؟"

---

## 🎯 شرح Regex:

### النمط الأساسي:
```
(?:افتح|شغل)    → مجموعة اختيار (افتح أو شغل)
\\s+            → مسافة واحدة أو أكثر
([^،,\\n]+?)    → مجموعة الالتقاط: أي شيء ما عدا الفواصل
(?=...)         → look-ahead: توقف عند الفاصل
```

### مثال بالتفصيل:
```
النص: "لو سمحت افتح واتساب، على الصوت"

Regex: (?:افتح|شغل)\\s+([^،,\\n]+?)(?=\\s*[،,\\n]|$)

الخطوات:
1. يبحث عن "افتح" أو "شغل" ✅ وجد "افتح"
2. يبحث عن مسافة ✅ وجد " "
3. يلتقط كل شيء حتى الفاصلة ✅ التقط "واتساب"
4. يتوقف عند "،"

النتيجة: "افتح واتساب"
```

---

## 💡 المميزات:

### ✅ لا حاجة لحذف كلمات محددة
```
النص: "أنا أريد أن أفتح واتساب من فضلك يا رب"
Regex: يستخرج "افتح واتساب" مباشرة
لا يحتاج: قائمة بالكلمات المهذبة
```

### ✅ يتجاهل كل شيء آخر تلقائياً
```
النص: "xyz123 افتح واتساب abc456"
Regex: يستخرج "افتح واتساب" فقط
يتجاهل: xyz123 و abc456
```

### ✅ يعمل مع أي كلام إضافي
```
النص: "والله يا أخي أريد افتح واتساب الله يخليك"
Regex: يستخرج "افتح واتساب"
لا يهمه: أي كلام إضافي
```

### ✅ دقيق في الاستخراج
```
النص: "افتح واتساب، على الصوت، شغل الواي فاي"
Regex 1: "افتح واتساب" (يتوقف عند الفاصلة)
Regex 2: "على الصوت" (يتوقف عند الفاصلة)
Regex 3: "شغل الواي فاي" (يتوقف عند نهاية النص)
```

---

## 📊 المقارنة:

| الطريقة | النهج | مشكلة |
|---------|-------|-------|
| **حذف كلمات** | حذف قائمة محددة | لا يمكن حذف كل الكلمات الممكنة |
| **Regex** | استخراج الأمر مباشرة | يتجاهل كل شيء آخر تلقائياً ✅ |

---

## 🎉 الخلاصة:

### النهج الجديد:
```
✅ يستخدم Regex لاستخراج الأوامر
✅ يتجاهل كل شيء آخر تلقائياً
✅ لا حاجة لقائمة كلمات للحذف
✅ دقيق وسريع
✅ يعمل مع أي كلام إضافي
```

### الكود:
```kotlin
// يبحث عن جميع الأنماط في النص
for ((type, pattern) in commandPatterns) {
    val matches = pattern.findAll(text)
    for (match in matches) {
        // يستخرج الأمر فقط
        val command = extractCommand(match)
        
        // يتحقق من صحته
        if (isValidCommand(command)) {
            commands.add(command)
        }
    }
}
```

**النتيجة: استخراج ذكي وتلقائي للأوامر!** 🎯🧠
