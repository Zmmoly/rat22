# 🚀 الأوامر المتقدمة - أواب AI

## ✨ المميزات الجديدة مع Accessibility Service

بعد تفعيل خدمة إمكانية الوصول، يمكن للتطبيق تنفيذ أوامر متقدمة:

---

## 📱 التحكم في التطبيقات

### إغلاق التطبيقات
```
أقفل واتساب
اقفل يوتيوب
```
- يفتح Recent Apps
- يبحث عن التطبيق
- يسحب لأعلى لإغلاقه

---

## 🎮 التحكم في النظام

### زر الرجوع
```
رجوع
ارجع
back
```

### الشاشة الرئيسية
```
الشاشة الرئيسية
هوم
home
```

### التطبيقات الأخيرة
```
التطبيقات الأخيرة
recent
recents
```

### الإشعارات
```
افتح الإشعارات
الاشعارات
```

---

## ⚙️ الإعدادات السريعة

### واي فاي (طريقة ذكية)
```
شغل الوايفاي    → يفتح الإعدادات السريعة ويضغط على الواي فاي
اطفي الوايفاي
```

### بلوتوث (طريقة ذكية)
```
شغل البلوتوث    → يفتح الإعدادات السريعة ويضغط على البلوتوث
اطفي البلوتوث
```

---

## 📸 لقطة الشاشة (Android 11+)

```
سكرين شوت
لقطة شاشة
screenshot
```
- يستخدم Accessibility API
- يحفظ الصورة تلقائياً
- يعرض إشعار بالنجاح

---

## 📖 قراءة محتوى الشاشة

```
اقرا الشاشة
ماذا في الشاشة
read screen
```
- يقرأ كل النصوص الظاهرة
- مفيد لمعرفة ما في الشاشة
- يمكن استخدامه للأتمتة

---

## 👆 الضغط على العناصر

```
اضغط على "موافق"
انقر على "التالي"
اضغط على "إرسال"
```
- يبحث عن العنصر بالنص
- يضغط عليه تلقائياً
- مفيد للأتمتة

---

## 🔧 كيف يعمل؟

### MyAccessibilityService

```kotlin
// الضغط على عنصر
service.clickByText("موافق")

// قراءة الشاشة
val text = service.getScreenText()

// إغلاق تطبيق
service.closeAppByName("واتساب")

// سكرين شوت (Android 11+)
service.takeScreenshot { success ->
    // تم أخذ السكرين شوت
}

// أوامر النظام
service.performBack()
service.performHome()
service.performRecents()
service.performNotifications()
service.performQuickSettings()

// تشغيل/إيقاف من الإعدادات السريعة
service.toggleWifiFromQuickSettings()
service.toggleBluetoothFromQuickSettings()
```

---

## ⚠️ متطلبات

### 1. تفعيل خدمة إمكانية الوصول
```
الإعدادات → إمكانية الوصول → أواب AI → تشغيل
```

### 2. الأذونات المطلوبة
- ✅ إمكانية الوصول (Accessibility)
- ✅ الأذونات العادية (من صفحة الإعدادات)

### 3. إصدار Android
- معظم الأوامر تعمل على Android 7+
- السكرين شوت يحتاج Android 11+

---

## 📋 قائمة الأوامر الكاملة

### 🟢 تعمل بدون Accessibility:
- افتح [تطبيق]
- اتصل [رقم]
- على الصوت / خفض الصوت / كتم
- فتح إعدادات (واي فاي، بلوتوث، إلخ)

### 🔵 تعمل مع Accessibility:
- أقفل [تطبيق]
- رجوع / هوم / recent
- افتح الإشعارات
- شغل/اطفي الوايفاي (من Quick Settings)
- شغل/اطفي البلوتوث (من Quick Settings)
- سكرين شوت (Android 11+)
- اقرا الشاشة
- اضغط على [نص]

---

## 🎯 أمثلة استخدام متقدمة

### مثال 1: إغلاق واتساب بعد إرسال رسالة
```
1. افتح واتساب
2. [أرسل الرسالة يدوياً]
3. أقفل واتساب
```

### مثال 2: تشغيل الواي فاي بسرعة
```
شغل الوايفاي
→ يفتح Quick Settings ويضغط تلقائياً
```

### مثال 3: أخذ سكرين شوت وقراءة محتواه
```
1. سكرين شوت
2. اقرا الشاشة
```

### مثال 4: أتمتة الضغط على أزرار
```
1. افتح الإعدادات
2. اضغط على "الواي فاي"
3. اضغط على "التوصيلات"
```

---

## 💡 نصائح

1. **تفعيل الخدمة ضروري**: معظم الأوامر المتقدمة لن تعمل بدون Accessibility
2. **الصبر**: بعض الأوامر تحتاج ثانية أو ثانيتين للتنفيذ
3. **الدقة في الأسماء**: عند الضغط على عنصر، استخدم النص الدقيق
4. **تجربة الأوامر**: جرب الأوامر لمعرفة كيف تعمل

---

## 🚀 المستقبل

قريباً:
- [ ] دعم الماكروهات (تنفيذ عدة أوامر تلقائياً)
- [ ] التحكم الصوتي
- [ ] الأتمتة المجدولة
- [ ] التحكم في التطبيقات عن بُعد

---

**استمتع بالتحكم الكامل في هاتفك!** 🎉
