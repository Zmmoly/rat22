# Android Permissions App

تطبيق Android بسيط يطلب جميع الأذونات الممكنة على Android 15 (API Level 35).

## الميزات

- ✅ طلب جميع الأذونات الخطرة (Dangerous Permissions)
- ✅ طلب الأذونات الخاصة (Special Permissions)
- ✅ خدمة إمكانية الوصول (Accessibility Service) مع جميع الميزات
- ✅ متوافق تمامًا مع Android 15
- ✅ جاهز للبناء على GitHub Actions

## الإعداد السريع 🚀

### للبناء على GitHub Actions (موصى به)
1. ارفع الملفات إلى مستودع GitHub جديد
2. GitHub Actions سيبني التطبيق تلقائياً
3. لن تحتاج لأي إعداد إضافي - الـ workflow يحمل gradle wrapper تلقائياً!

### للبناء المحلي
قبل البناء، قم بتحميل gradle wrapper:
```bash
./download_wrapper.sh
```

أو يدوياً:
```bash
curl -L -o gradle/wrapper/gradle-wrapper.jar \
  https://raw.githubusercontent.com/gradle/gradle/v8.2.1/gradle/wrapper/gradle-wrapper.jar
```

ثم:
```bash
./gradlew assembleRelease
```

📖 **للمزيد من التفاصيل، راجع ملف [SETUP.md](SETUP.md)**

## الأذونات المدعومة

### أذونات خطرة (Dangerous Permissions)
- التقويم (Calendar)
- الكاميرا (Camera)
- جهات الاتصال (Contacts)
- الموقع (Location - بما في ذلك الموقع في الخلفية)
- الميكروفون (Microphone)
- الهاتف (Phone)
- المستشعرات (Sensors)
- الرسائل القصيرة (SMS)
- التخزين والوسائط (Storage & Media)
- التعرف على النشاط (Activity Recognition)
- البلوتوث (Bluetooth)
- الإشعارات (Notifications)
- الأجهزة القريبة (Nearby Devices)

### أذونات خاصة (Special Permissions)
- الرسم فوق التطبيقات الأخرى
- تعديل إعدادات النظام
- إدارة جميع الملفات
- تثبيت الحزم
- تجاهل تحسين البطارية

### خدمة إمكانية الوصول
- الوصول إلى جميع الأحداث
- إمكانية تنفيذ الإيماءات
- الوصول إلى محتوى النوافذ
- جميع ميزات إمكانية الوصول المتاحة

## البناء

### محليًا
```bash
./gradlew assembleRelease
```

### على GitHub Actions
قم برفع الكود إلى GitHub وسيتم بناء التطبيق تلقائيًا.

## المتطلبات

- Android Studio Arctic Fox أو أحدث
- JDK 17
- Android SDK API Level 35
- Gradle 8.2.0 أو أحدث

## الحزمة
```
com.awab.ai
```

## ⚠️ تنبيه أمني مهم

هذا التطبيق يستخدم **debug keystore** للتوقيع للأغراض التعليمية فقط.
- **لا تستخدم** هذا الـ keystore في تطبيقات الإنتاج
- **قم بإنشاء** keystore خاص بك لتطبيقات حقيقية
- معلومات التوقيع:
  - Store Password: `android`
  - Key Alias: `androiddebugkey`
  - Key Password: `android`

## ⚠️ تنبيه حول خدمة إمكانية الوصول

🔥 **الخدمة تطلب جميع الصلاحيات - جاهزة لإضافة الوظائف**

### الحالة الحالية:
- ✅ تطلب **جميع صلاحيات** إمكانية الوصول من النظام
- ✅ جاهزة للاستخدام من خلال `MyAccessibilityService.getInstance()`
- ⚠️ الوظائف موجودة لكن **غير منفذة** (TODO)
- ❌ **لا تستمع أو تسجل** أي شيء حالياً

### الصلاحيات المطلوبة:
- النقر والسحب التلقائي (`canPerformGestures`)
- قراءة محتوى الشاشة (`canRetrieveWindowContent`)
- أخذ لقطات شاشة (`canTakeScreenshot`)
- جميع أنواع الأحداث (`typeAllMask`)
- التحكم في التكبير (`canControlMagnification`)
- طلب بصمة الإصبع (`canRequestFingerprint`)
- وأكثر...

### كيفية الاستخدام:
```kotlin
val service = MyAccessibilityService.getInstance()
service?.performClick(x, y)  // سيتم التنفيذ عند إضافة الكود
```

📖 **راجع `ACCESSIBILITY_API.md` لقائمة كاملة بالوظائف المتاحة**

### لإضافة أزرار وتنفيذ الوظائف:
1. افتح `MainActivity.kt` وأضف الأزرار
2. افتح `MyAccessibilityService.kt` ونفذ الوظائف المطلوبة
3. استخدم `getInstance()` للوصول للخدمة

## ملاحظات هامة

1. التطبيق يستخدم `targetSdk = 35` (Android 15)
2. جميع الأذونات متوافقة مع متطلبات Android 15
3. خدمة إمكانية الوصول مُهيأة بجميع الميزات الممكنة
4. APK المُنتج غير موقّع - يحتاج للتوقيع قبل النشر

## الترخيص
MIT License
